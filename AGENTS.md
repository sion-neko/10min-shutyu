# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

This project is pinned to **SDK 54** because it is tested on Expo Go 54.0.2. Do not upgrade the
SDK without the Expo Go app being upgraded first.
